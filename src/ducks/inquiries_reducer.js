//import axios from 'axios';

let initialState = {
  inquiries: {
    userId: null,
    itemList: []
  }
};

export default function inquiries_reducer(state = initialState, action){
  switch(action.type){
    default:
      return state;
  }
};
